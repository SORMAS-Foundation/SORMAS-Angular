/* tslint:disable:no-unused-variable */
import { ToShortIdPipe } from './toShortId.pipe';

describe('Pipe: ToShortId', () => {
  it('create an instance', () => {
    const pipe = new ToShortIdPipe();

    expect(pipe).toBeTruthy();
  });
});
