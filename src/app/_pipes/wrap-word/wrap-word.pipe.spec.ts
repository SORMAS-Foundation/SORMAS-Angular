/* eslint-disable @typescript-eslint/no-unused-vars */
import { WrapWordPipe } from './wrap-word.pipe';

describe('Pipe: Wrap word', () => {
  it('create an instance', () => {
    const pipe = new WrapWordPipe();

    expect(pipe).toBeTruthy();
  });
});
