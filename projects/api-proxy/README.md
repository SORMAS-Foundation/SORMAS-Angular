The purpose of this proxy is to have this used in Sormas Angular local environment only.
It is mainly to bypass CORS issues that won't arrise on production environment once the Angular app is deployed to the same domain.

## Install dependencies

`npm i`

## Run the Proxy server

`npm start`
